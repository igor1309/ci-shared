# Log

## 2026-03-01

- Add optional `TELEGRAM_PARSE_MODE` env var to `notify_telegram.sh`, enabling callers to specify Markdown, MarkdownV2, or HTML. Backward compatible — plain text when unset. ADR-001 updated.
